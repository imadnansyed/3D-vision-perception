step1: Create architechture for storing the features => feature_set.py
step2: Load images and each image have 4 attributes including id, name, image, gray => image_loader.py
step3: Extract features and compute the descriptors and store in list of dicts containing feature architectures created in step 1